# 二分查找

二分最容易错的不是 `mid`，而是左右边界的语义。先明确区间是 `[l, r]` 还是 `[l, r)`，再写 `while` 条件和更新规则。

- 找左边界：满足条件后继续向左收缩。
- 找右边界：满足条件后继续向右收缩。
- 不要在同一份模板里混用闭区间和半开区间。

```cpp
while (l <= r) {
    int mid = l + (r - l) / 2;
    if (check(mid)) r = mid - 1;
    else l = mid + 1;
}
```
